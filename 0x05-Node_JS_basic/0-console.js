/**
 * Shows message on the STDOUT.
 * @param {String} msg Shows message.
 * @author Graham S. Paul <gpaul988@gmail.com>
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;